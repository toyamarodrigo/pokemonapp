import React from 'react';
import { BasicLayout } from '../../components';

export const FavoritePage = ({ pokemon, setPokemon, setFavorite }) => {
  return (
    <BasicLayout>
      <h1>FavoritePages</h1>
    </BasicLayout>
  );
};
